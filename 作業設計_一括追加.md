# 作業設計: cultural.jp「品入札」334件の一括追加（1日5件ずつ）

作成: 2026-09-11（Fable による設計。実装と日次運用は下位モデルが行う）

## 1. 目的と範囲

Cultural Japan の検索「品入札」（https://cultural.jp/search?keyword=品入札 、334件）に表示される
入札目録を、本サイト（売立目録全文検索システム、https://dichika.github.io/uritate-mokuroku-search/ ）に
1日5件ずつ追加していく。ユーザーの指示は「急がない、1日5件ずつくらい」である。

### 1.1 対象の内訳（2026-09-11 に api.cultural.jp で確認）

| 提供元 | 件数 | IIIF | 扱い |
|---|---|---|---|
| ARC古典籍ポータルデータベース（UC Berkeley 192件、セインズベリー日本藝術研究所 126件） | 318 | あり（dh-jac.net マニフェスト、画像は arc.ritsumei.ac.jp） | **第1段階の対象**。既存パイプラインがそのまま使える |
| 国立国会図書館デジタルコレクション | 7 | 2件あり（dignl-949964, 949965 大谷家旧御蔵品入札目録）、他は官報3件・IIIFなし2件 | IIIFあり2件は**第2段階**。官報3件は入札目録ではないので除外 |
| オーテピア高知図書館 | 4 | なし | 除外（画像取得手段を個別調査する必要があり、今回の範囲外） |
| 山梨デジタルアーカイブ | 2 | なし | 除外（「厩使用の品入札案紙」など文書であり売立目録ではない） |
| 佐賀県立図書館 | 2 | なし | 除外 |
| ADEAC | 1 | なし | 除外 |

対象は 318 + 2 = **320件**である。除外した14件は `data/catalog/queue.json` に status=skipped と理由を残す。

### 1.2 データ取得先とその挙動（検証済み）

- 一覧: `https://api.cultural.jp/search?keyword=品入札&size=100&page=N`（N=1..4）。
  応答は `hits.total.value` と `hits.hits[]`（id, manifest[], image[], title.ja[], source.ja[], access.ja[], temporal.ja[]）。
  スナップショットを `data/catalog/cultural_jp_hits.json` に保存済み（334件）。
- ARC のマニフェスト: `https://www.dh-jac.net/db1/books/{book_id}/portal/manifest.json`。
  book_id は id の `arc_books-` 以降を `_`→`-` に置換したもの（例: `arc_books-SISJAC_AC_014_` → `SISJAC-AC-014-`、末尾ハイフン付きあり）。
  manifest 欄の URL をそのまま使えばよい（334件すべてで規則が一致することを確認済み）。
  **注意**: このホストは自動アクセスに HTTP 429（Retry-After 3600）を返すことがある。
  2026-09-11 の検証では、ブラウザ相当の User-Agent で `.../portal/` を先に GET してクッキーを受け取り、
  同じクッキーで manifest.json を取ると 200 で取得できた。429 が返ったらその回の追加は中止し、翌日に持ち越す。
- ARC の画像: manifest 内 `service.@id` + `/full/full/0/default.jpg`。UA に `Claude/` や `curl/` を含むと 403。
  `config.USER_AGENT`（Chrome相当）で取得する。1リクエストごとに `config.REQUEST_WAIT_SEC` 待つ。
- NDL（第2段階）: `https://www.dl.ndl.go.jp/api/iiif/{pid}/manifest.json`。画像は `https://dl.ndl.go.jp/api/iiif/{pid}/R0000001` 形式の
  IIIF Image API（info.json 200 を確認）。metadata の label が ARC と異なる（Title, Publication Date, Call Number, DOI）。

### 1.3 規模の見積

1冊あたり 60〜140コマ（UCB-0066 は62、SISJAC-AC-014 は97、UCB-0146 は136）。
320冊 × 約100コマ ≒ 32,000ページ、OCR行は1ページ6行前後なので約20万行になる。
現在の `docs/data/index.json` は136ページで78KB なので、単一ファイルのままでは 20MB 前後に達し、
スマホでの初回読み込みに耐えない。よってインデックスを分割する（§3）。

## 2. 実装項目（下位モデルが実装する）

### 2.1 `pipeline/fetch_catalog.py`（新規）

- api.cultural.jp から「品入札」全件を取得し `data/catalog/queue.json` を生成・更新する。
- queue.json の各要素: `{id, book_id, title, manifest, source, access, temporal, provider: "arc"|"ndl"|"other", status: "pending"|"done"|"skipped"|"error", note, updated}`。
- 再実行時は既存の status/note を保持する（冪等）。§1.1 の除外規則で `skipped` を初期設定する。
- `book_id` は ARC なら manifest URL の `/books/` と `/portal/` の間、NDL なら `ndl-{pid}` とする。

### 2.2 `config.py` の変更

- `BOOK_IDS` の固定リストをやめ、`data/catalog/queue.json` の status=done の book_id を返す関数 `done_book_ids()` を追加する。
  既存の `UCB-N7352-B558-0146` は queue.json 上で status=done にしておく（cultural.jp の一覧にも含まれている: `arc_books-UCB_N7352_B558_0146`）。
- `fetch.py` の `MANIFEST_URL_TMPL` は ARC 用のまま残し、NDL 用のテンプレートを別途追加する。
- 追加の定数: `QUEUE_JSON`, `LOG_DIR`, `DAILY_BATCH_SIZE = 5`。

### 2.3 `pipeline/add_books.py`（新規・日次実行の本体）

```
python3 pipeline/add_books.py --count 5 [--push] [--book-id XXX]
```

1. queue.json から status=pending かつ provider=arc のものを先頭から `--count` 件取る（第2段階で ndl も対象にする）。
2. 各冊について順に: マニフェスト取得（§1.2 のクッキー手順。429 なら即中止し status はそのまま、error ではない）→
   画像取得（既存 `fetch.py` の関数を再利用）→ OCR（既存 `run_ocr.py` の関数を再利用。1冊ずつ）→ status=done。
   途中で例外が出た冊は status=error と note に例外を記録し、次の冊へ進む。
3. 全冊処理後に `build_index.py` を実行してインデックスを再生成する。
4. `--push` 指定時は `git add -A && git commit -m "資料追加: <title>×N" && git push origin main`。
5. 結果を `logs/add_books_YYYYMMDD.log` に追記し、標準出力にも「追加 N冊 / エラー M冊 / 残り pending K冊」を1行で出す。
6. 同時二重起動を避けるため `logs/add_books.lock` を使う（存在すれば即終了）。

### 2.4 インデックス分割と検索UIの対応

- `docs/data/catalog.json`: 全冊のメタデータ一覧（id, title, owner, year, code, pages数, detailUrl, viewerUrl）。
- `docs/data/search.json`: 検索用の軽量データ。冊ごとに `{"id":..., "p":[[コマ番号, "行テキスト"], ...]}`。座標は含めない。
- `docs/data/books/{book_id}.json`: 現在の1冊分の構造（pages[].lines[].b 付き）。オーバーレイを開いたときに遅延読み込みする。
- `docs/index.html`: 起動時は catalog.json と search.json だけを読む。検索ヒット→オーバーレイ表示時に books/{id}.json を fetch してキャッシュする。
  資料一覧（検索語なし）は320冊になるので、所蔵（UC Berkeley / SISJAC / NDL）と年代で絞り込むセレクトと、
  タイトルの部分一致による絞り込みを追加する。カード数が多いので最初は50件表示し「さらに表示」で追加する。
- 既存の旧字体正規化（kanji-variants.js）とハイライトはそのまま使う。search.json は gzip 配信で 1/3 程度になる見込み。

### 2.5 テスト

- `add_books.py --count 2` を手動実行し、2冊が追加されて `docs/index.html` で検索・画像表示ができることを
  通常の Chrome で確認する（Claude Code のアプリ内ブラウザは UA の都合で画像が出ない。README 参照）。
- 既存の UCB-N7352-B558-0146 が引き続き検索できること。

## 3. 日次運用（スケジュールタスク）

- taskId: `uritate-add-books-daily`、毎日 02:30（cron `30 2 * * *`）。
  根拠: 純粋な自動処理でユーザーの判断を要さないので夜間でよい（CLAUDE.md ルール9の例外）。
  02:02 の `kenpo-shinchoku-daily` は数分で終わり、毎月24日 03:20 の `kantoits-monthly-article`（約35分）とも
  5冊分（画像取得＋OCRで30〜40分）が重ならない時刻として選んだ。
- プロンプトは「`python3 pipeline/add_books.py --count 5 --push` を実行し、標準出力の最終行を報告する。
  429 で中止した場合はその旨を報告し再試行しない」だけにして、モデルの作業を最小にする。
- 320冊 ÷ 5冊/日 ≒ 64日で完了する見込み。完了後（pending が 0 になったら）タスクを無効化する。

## 4. 完了の定義

- queue.json の pending が 0（ARC 318件 + NDL 2件が done、または error に理由付き）。
- 公開サイトで全冊が検索でき、README の収録資料表が更新されている。
